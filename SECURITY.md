# 安全策略

知识补全项目（Knowledge Completion）当前处于 Active Prototype 阶段，尚未完成多用户认证、租户隔离、限流、审计日志和完整备份策略。请勿在未完成安全评审的部署中存放高敏感或受监管数据。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `main` 最新提交 | 支持 |
| 历史提交或个人分支 | 不保证 |

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue、讨论区或截图中披露：

- 可利用的漏洞细节；
- 访问令牌、Cookie、D1 数据或环境变量；
- 真实用户笔记正文；
- 能识别个人身份的日志；
- 可被直接复现的生产 URL 与攻击载荷组合。

报告应包含：

1. 受影响的提交或版本；
2. 漏洞类型与影响范围；
3. 最小复现步骤；
4. 是否需要已登录身份；
5. 可能的修复方向；
6. 你希望使用的署名方式。

项目维护者会尽快确认报告是否可复现，并在修复准备完成前限制漏洞信息传播。当前原型不承诺正式 SLA。

## 当前安全基线

- SQL 使用 D1 prepared statements；
- Run API 对长度、整数范围、枚举、关系端点和笔记证据原文进行服务端严格校验；
- 内容使用 SHA-256 唯一哈希避免重复写入；
- `.env*`、本地数据库、依赖和构建产物默认忽略；
- Demo 数据与用户数据来源分离；
- 数据库结构通过迁移审查。
- Agent 笔记输入按不可信数据处理，并与系统提示分离；
- 所谓“笔记证据”必须逐字出现在对应原文，否则请求被拒绝；无来源候选必须显式保持 boundary；
- Agent 同步运行限制笔记数量、总字符数、候选概念数、关系数和最终节点数；
- 浏览器 HTTP 入口不接收模型 API Key，CLI 仅从环境变量读取凭据；
- Plugin helper 验证服务身份、同源 dashboard URL 和持久化回读；远程全文上传必须显式确认。

## 已知待补安全能力

- 用户认证、授权与租户隔离；
- API 限流与边缘请求体限制；
- CSRF、CSP 与外部链接策略的系统化检查；
- 结构化安全日志与异常告警；
- 数据保留、导出、删除、备份和恢复流程；
- 依赖漏洞与供应链扫描；
- 外部网页抓取的 SSRF 防护与内容沙箱；
- 渗透测试和灾难恢复演练。

## 凭据处理

不要将任何访问令牌写入源码、Git remote URL、Issue、PR、测试快照或日志。运行时密钥应由部署平台的 Secret 管理能力提供。本开源仓库不提交任何旧部署的项目 ID；部署者应在自己的平台配置资源绑定。
